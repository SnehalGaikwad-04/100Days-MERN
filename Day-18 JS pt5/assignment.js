//Create an object representing a car that stores the following properties for the car: name, model, color. Print the car’s name.

const phone={
    name:'Samsung',
    model:'M51',
    color:'blue',
};
console.log(phone);

//Create an object Person with their name, age, and city. Edit their city’s original value to 
// change it to 'New York'. Add a new property country and set it to the United States.

const Person={
    name:'snehal',
    age:19,
    city:'mumbai'
};